local Messenger = class("Messenger")

function Messenger:add_listener(eventType, cs_delegate)
    CS.NextFramework.Messenger:
end
function Messenger:remove_listener(eventType, cs_delegate)
    
end
function Messenger:broadcast(eventType, ...)
    
end

return Messenger